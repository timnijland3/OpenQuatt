# ==============================================================================
# OpenQuatt - Thermal Request Control
# ==============================================================================
#
# Goal:
#   Consume strategy outputs, publish an explicit thermal request interface,
#   and prepare final actuator requests for HP1/HP2.
#
# Role in architecture:
#   Owns shared thermal-request control between strategy modules and the
#   downstream thermal actuator.
#
# What this package DOES do:
#   - Consumes oq_demand_raw (published by oq_strategy_manager)
#   - Consumes strategy outputs from oq_cooling_strategy / oq_heating_curve_strategy / oq_power_house_strategy
#   - Publishes `oq_request_*` as the explicit pre-actuator request interface
#   - Publishes finalized actuator requests for the downstream thermal actuator
#   - Owns shared request arbitration, guards, and actuator input logic
#   - Consumes shared HP capacity/deficit (`oq_P_hp_cap_w`, `oq_P_deficit_w`) for supervisory/debug logic
#
# What this package DOES NOT do:
#   - Has no dedicated temperature PID loop (strategy owns temperature-loop behavior)
#   - Does not implement flow interlock (flow-control + supervisory are leading)
#   - Does not determine Control Mode
#   - Does not control boiler directly (boiler follows CM3/CM4)
#   - Does not write HP working mode or compressor levels directly
#
# Key assumptions:
#   - Strategy request outputs are available before this package runs
#   - Compressor levels are `select` entities with options '0'..'10'
#
# Interaction with Control Modes:
#   - Active in CM2/CM3 (heating) and CM5 (cooling)
#   - In CM0/CM1/CM4/CM98/CM100, compressor levels are forced to 0
#   - Manual HP service may own the request while CM100 keeps circulation active
#
# Safety / fail-safe:
#   - With inactive CM: compressor levels 0 (no heat production)
#   - Thermal request control is always subordinate to flow interlock via CM1
#
# Runtime arbitration is implemented in oq_thermal_request_runtime.h; this YAML
# remains the configuration/entity contract and scheduler wiring.
#
# ==============================================================================

# ----------------------------
# INTERNAL STATE
# ----------------------------
globals:
  # Explicit pre-actuator thermal request interface.
  - id: oq_request_mode_code
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_request_hp1_level
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_request_hp2_level
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_request_owner_hp
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_request_topology_code
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_request_strategy_code
    type: int
    restore_value: false
    initial_value: '0'

  # Final per-HP requests consumed by oq_thermal_actuator.
  - id: oq_actuator_hp1_req
    type: int
    restore_value: false
    initial_value: '0'

  - id: oq_actuator_hp2_req
    type: int
    restore_value: false
    initial_value: '0'

  # Shared filtered demand owned by the active strategy module.
  - id: oq_demand_filtered
    type: int
    restore_value: false
    initial_value: '0'

  # Previous filtered demand sample published by the active strategy module.
  - id: oq_demand_filtered_prev
    type: int
    restore_value: false
    initial_value: '0'

  # Shared heating-only filtered demand, kept separate from CM5 cooling demand.
  - id: oq_heating_demand_filtered
    type: int
    restore_value: false
    initial_value: '0'

  # Power House telemetry (W)
  - id: oq_P_hp_cap_w
    type: float
    restore_value: false
    initial_value: '0.0'

  - id: oq_P_deficit_w
    type: float
    restore_value: false
    initial_value: '0.0'

  # Shared curve request state, currently published by the curve strategy.
  - id: oq_dual_hp_enabled
    type: bool
    restore_value: false
    initial_value: 'false'

  # Fractional elapsed minutes for dual-enable hold timing.
  - id: oq_dual_hp_enable_hold_elapsed_accum_min
    type: float
    restore_value: false
    initial_value: '0.0'
  - id: oq_dual_hp_disable_hold_elapsed_accum_min
    type: float
    restore_value: false
    initial_value: '0.0'

  # Emergency dual-enable hold timing (curve mode capacity/deficit override).
  - id: oq_dual_hp_emergency_hold_elapsed_accum_min
    type: float
    restore_value: false
    initial_value: '0.0'

  # Last loop timestamp for elapsed-time scaling.
  - id: oq_heat_last_loop_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'

  # Last control flavor observed by heat-control loop (0=Power House, 1=Heating Curve, 2=Cooling).
  - id: oq_heat_last_mode_code
    type: int
    restore_value: false
    initial_value: '-1'

  # Fractional runtime accumulation; converted to whole minutes.
  - id: hp1_runtime_accum_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: hp2_runtime_accum_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'

  # Per-HP timestamp of the last measured transition into working_mode=Heating.
  # Minimum runtime is enforced from this real heating start, not from an earlier
  # request/apply transition that can happen before the compressor truly runs.
  - id: hp1_last_real_heat_start_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: hp2_last_real_heat_start_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: hp1_prev_heating_mode_active
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: hp2_prev_heating_mode_active
    type: bool
    restore_value: false
    initial_value: 'false'

  # Diagnostic: last computed lead pump based on runtime minutes.
  - id: oq_last_lead_hp
    type: int
    restore_value: false
    initial_value: '0'

  # Latched single-HP owner in heating-curve mode (0=none, 1=HP1, 2=HP2).
  # This prevents lead handovers while demand is continuously active.
  - id: oq_curve_single_owner_hp
    type: int
    restore_value: false
    initial_value: '0'

  # Per-HP timestamp for explicit level slew limiting.
  - id: hp1_last_level_change_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: hp2_last_level_change_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'

  # Holds the current single-vs-dual choice briefly after an actual topology change.
  - id: oq_duo_request_hold_until_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: oq_boot_startup_inhibit_logged
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: oq_boot_startup_inhibit_cleared_logged
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: oq_boot_startup_inhibit_event_active
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: oq_boot_startup_inhibit_event_started_ms
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: oq_boot_startup_inhibit_event_subject
    type: uint8_t
    restore_value: false
    initial_value: '0'
  - id: oq_boot_startup_inhibit_event_mode
    type: uint8_t
    restore_value: false
    initial_value: '0'

# ----------------------------
# TUNING INPUTS
# ----------------------------
select:
  # Controls when the second ODU joins in curve (Water Temperature Control) mode.
  # Sequential keeps the historical behaviour: the lead ODU rides up to (near)
  # its own maximum before the second unit is even considered. Share Load caps
  # the lead ODU at "Share Load Start Level" and hands any demand beyond that
  # to the second ODU instead, so load is split between both units sooner.
  - platform: template
    id: oq_duo_dispatch_mode
    name: "Duo Dispatch Mode"
    internal: ${hc_dual_tuning_internal}
    disabled_by_default: true
    icon: mdi:swap-horizontal-bold
    optimistic: true
    restore_value: true
    options:
      - "1 Running / 1 Standby"
      - "Share Load"
    initial_option: "1 Running / 1 Standby"
    entity_category: config

number:
  - platform: template
    id: oq_min_runtime_min
    name: "Minimum runtime"
    icon: mdi:timer-cog-outline
    entity_category: config
    unit_of_measurement: "s"
    min_value: ${oq_hp_min_runtime_min_s}
    max_value: 3600
    step: 30
    mode: slider
    restore_value: true
    optimistic: true
    initial_value: 300

  # Hold time before enabling dual-HP in curve mode.
  - platform: template
    id: oq_dual_hp_enable_hold_min
    name: "Dual HP Enable Hold"
    internal: ${hc_dual_tuning_internal}
    disabled_by_default: true
    entity_category: config
    icon: mdi:timer-plus-outline
    unit_of_measurement: "min"
    min_value: 1
    max_value: 60
    step: 1
    mode: slider
    restore_value: true
    optimistic: true
    initial_value: 3

  # Hold time before disabling dual-HP in curve mode.
  - platform: template
    id: oq_dual_hp_disable_hold_min
    name: "Dual HP Disable Hold"
    internal: ${hc_dual_tuning_internal}
    disabled_by_default: true
    entity_category: config
    icon: mdi:timer-minus-outline
    unit_of_measurement: "min"
    min_value: 1
    max_value: 60
    step: 1
    mode: slider
    restore_value: true
    optimistic: true
    initial_value: 5

  # Lead-ODU level at which "Share Load" mode hands further demand to the
  # second ODU instead of raising the lead further. Ignored in
  # "1 Running / 1 Standby" mode. Floored at 2: running both ODUs at level 1
  # each gains too little capacity to be worth the extra cycling.
  - platform: template
    id: oq_duo_share_load_start_level
    name: "Share Load Start Level"
    internal: ${hc_dual_tuning_internal}
    disabled_by_default: true
    entity_category: config
    icon: mdi:arrow-up-bold-hexagon-outline
    unit_of_measurement: "lvl"
    min_value: 2
    max_value: 9
    step: 1
    mode: slider
    restore_value: true
    optimistic: true
    initial_value: 2

# ----------------------------
# BUTTON
# ----------------------------
button:
  - platform: template
    id: oq_reset_runtime
    name: "${hc_runtime_reset_button_name}"
    icon: mdi:restart
    entity_category: config
    disabled_by_default: true
    on_press:
      - lambda: |-
          // Reset accumulated runtime counters and measured-start latches together,
          // so fairness and minimum-runtime diagnostics restart from a clean state.
          #if OQ_TOPOLOGY_DUO
          id(hp1_minutes) = 0;
          id(${secondary_minutes_id}) = 0;
          id(hp1_runtime_accum_ms) = 0;
          id(hp2_runtime_accum_ms) = 0;
          id(hp1_last_real_heat_start_ms) = 0;
          id(hp2_last_real_heat_start_ms) = 0;
          id(hp1_prev_heating_mode_active) = false;
          id(hp2_prev_heating_mode_active) = false;
          ESP_LOGI("quatt", "Runtime counters reset at boot (HP1+HP2).");
          #else
          id(hp1_minutes) = 0;
          id(hp1_runtime_accum_ms) = 0;
          id(hp1_last_real_heat_start_ms) = 0;
          id(hp1_prev_heating_mode_active) = false;
          ESP_LOGI("quatt", "Runtime counters reset at boot (HP1).");
          #endif

# ----------------------------
# SENSORS (energie + status)
# ----------------------------
sensor:
  - platform: template
    id: oq_total_power_input
    name: "Total Power Input"
    icon: mdi:flash
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 2
    update_interval: 10s
    lambda: |-
      return oq_energy_runtime::total_power_input();

  - platform: template
    id: oq_heating_power_input
    name: "Heating Power Input"
    icon: mdi:flash
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 2
    update_interval: 10s
    lambda: |-
      return oq_energy_runtime::heating_power_input();

  - platform: template
    id: oq_cooling_power_input
    name: "Cooling Power Input"
    icon: mdi:flash
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 2
    update_interval: 10s
    lambda: |-
      return oq_energy_runtime::cooling_power_input();

  - platform: template
    id: oq_total_heat_power
    name: "Total Heat Power"
    icon: mdi:radiator
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 10s
    lambda: |-
      return oq_energy_runtime::total_heat_power();

  - platform: template
    id: oq_total_cooling_power
    name: "Total Cooling Power"
    icon: mdi:snowflake
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 10s
    lambda: |-
      return oq_energy_runtime::total_cooling_power();

  - platform: template
    id: oq_total_cop
    name: "Total COP"
    icon: mdi:chart-bell-curve-cumulative
    state_class: measurement
    accuracy_decimals: 2
    update_interval: 10s
    lambda: |-
      return oq_energy::instant_ratio_or_nan(id(oq_total_heat_power).state, id(oq_total_power_input).state, ${oq_cop_min_input_w});

  - platform: template
    id: oq_total_eer
    name: "Total EER"
    icon: mdi:chart-bell-curve-cumulative
    state_class: measurement
    accuracy_decimals: 2
    update_interval: 10s
    lambda: |-
      return oq_energy::instant_ratio_or_nan(id(oq_total_cooling_power).state, id(oq_cooling_power_input).state, ${oq_cop_min_input_w});

  - platform: template
    id: oq_demand_raw_sensor
    name: "Demand raw"
    icon: mdi:chart-line
    unit_of_measurement: "step"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: ${oq_diagnostic_update_interval_s}s
    lambda: |-
      return (float) id(oq_demand_raw);

  - platform: template
    id: oq_demand_filtered_sensor
    name: "Demand filtered"
    icon: mdi:chart-line-variant
    unit_of_measurement: "step"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: ${oq_diagnostic_update_interval_s}s
    lambda: |-
      return (float) id(oq_demand_filtered);

  - platform: template
    id: oq_hp1_compressor_level_sensor
    name: "HP1 compressor level"
    icon: mdi:stairs
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      if (!id(hp1_compressor_level).has_state()) return NAN;
      auto idx = id(hp1_compressor_level).active_index();
      if (!idx.has_value()) return NAN;
      return (float) idx.value();

  - platform: template
    id: oq_hp2_compressor_level_sensor
    name: "HP2 compressor level"
    internal: ${hc_hp2_diag_internal}
    icon: mdi:stairs
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      #if OQ_TOPOLOGY_DUO
      if (!id(${secondary_compressor_level_id}).has_state()) return NAN;
      auto idx = id(${secondary_compressor_level_id}).active_index();
      if (!idx.has_value()) return NAN;
      return (float) idx.value();
      #else
      return NAN;
      #endif

  # V2/Power-House debug chain (recorder-only, no control behavior change).
  # Requested/applied use the 0..10 model/control domain; physical F-level is
  # already recorded via "HPx compressor level" (0..20, register 1999) and
  # measured Hz via "HPx - Compressor frequency". Table frequency resolves the
  # last commanded physical F-level through the validated heating runtime
  # table. All return NAN when unavailable; never invent values. HP2 returns
  # NAN on Single (fail-closed).
  - platform: template
    id: oq_hp1_requested_control_level_sensor
    name: "HP1 requested control level"
    internal: true
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      return (float) id(oq_actuator_hp1_req);

  - platform: template
    id: oq_hp1_applied_control_level_sensor
    name: "HP1 applied control level"
    internal: true
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      return (float) id(hp1_last_applied_level);

  - platform: template
    id: oq_hp1_table_frequency_sensor
    name: "HP1 table frequency"
    internal: true
    unit_of_measurement: "Hz"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      const auto snapshot = oq_odu::decode_runtime_frequency_snapshot(id(hp1_runtime_frequency_snapshot_storage));
      const int physical = id(hp1_last_commanded_physical_level);
      if (physical == 0) return 0.0f;
      if (physical < 0 || !snapshot.heating.valid) return NAN;
      const int hz = oq_odu::frequency_for_physical_level(snapshot.heating, physical);
      return hz > 0 ? (float) hz : NAN;

  - platform: template
    id: oq_hp2_requested_control_level_sensor
    name: "HP2 requested control level"
    internal: true
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      #if OQ_TOPOLOGY_DUO
      return (float) id(oq_actuator_hp2_req);
      #else
      return NAN;
      #endif

  - platform: template
    id: oq_hp2_applied_control_level_sensor
    name: "HP2 applied control level"
    internal: true
    unit_of_measurement: "lvl"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      #if OQ_TOPOLOGY_DUO
      return (float) id(${secondary_last_applied_level_id});
      #else
      return NAN;
      #endif

  - platform: template
    id: oq_hp2_table_frequency_sensor
    name: "HP2 table frequency"
    internal: true
    unit_of_measurement: "Hz"
    state_class: measurement
    accuracy_decimals: 0
    update_interval: 5s
    lambda: |-
      #if OQ_TOPOLOGY_DUO
      const auto snapshot =
          oq_odu::decode_runtime_frequency_snapshot(id(${secondary_runtime_frequency_snapshot_storage_id}));
      const int physical = id(${secondary_last_commanded_physical_level_id});
      if (physical == 0) return 0.0f;
      if (physical < 0 || !snapshot.heating.valid) return NAN;
      const int hz = oq_odu::frequency_for_physical_level(snapshot.heating, physical);
      return hz > 0 ? (float) hz : NAN;
      #else
      return NAN;
      #endif

# ----------------------------
# Extra overview sensors for capacity deficit
# ----------------------------

  - platform: template
    id: oq_hp_capacity_w_sensor
    name: "HP capacity (W)"
    icon: mdi:radiator
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 0
    update_interval: ${oq_diagnostic_update_interval_s}s
    lambda: |-
      return id(oq_P_hp_cap_w);

  - platform: template
    id: oq_hp_deficit_w_sensor
    name: "HP deficit (W)"
    icon: mdi:alert-circle-outline
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 0
    update_interval: ${oq_diagnostic_update_interval_s}s
    lambda: |-
      return id(oq_P_deficit_w);

text_sensor:
  - platform: template
    id: oq_request_reason
    name: "Request Reason"
    internal: true
    update_interval: never
    lambda: |-
      return std::string("inactive");

  - platform: template
    id: oq_runtime_lead_hp_sensor
    name: "Runtime lead HP"
    icon: mdi:source-branch
    update_interval: 10s
    lambda: |-
      #if OQ_TOPOLOGY_DUO
      const bool lead_is_hp1 = (id(hp1_minutes) <= id(${secondary_minutes_id}));
      return std::string(lead_is_hp1 ? "HP1" : "HP2");
      #else
      return std::string("HP1");
      #endif

# ----------------------------
# MAIN LOOP
# ----------------------------
interval:
  - interval: ${oq_heat_loop_tick_s}s
    startup_delay: 4000ms
    then:
      - lambda: |-
          oq_thermal_request_runtime::runtime().tick({
              (uint32_t) millis(),
              (uint32_t) ${oq_heat_loop_tick_s} * 1000UL,
              (uint32_t) ${oq_heat_loop_curve_s} * 1000UL,
              (uint32_t) ${oq_heat_loop_powerhouse_s} * 1000UL,
              (uint32_t) ${oq_hp_min_off_s},
              (int) ${oq_hp_min_runtime_min_s},
              (int) ${oq_strategy_demand_max_f},
              (float) ${oq_cm_min_flow_lph},
          });
